- Figure out updates (probably compare checksums and merge files that don't match, deal with files in etc seperately. files in bin should be merged automatically)
- Become distro-agnostic (need hooks for other package managers)
- Something something save kernels before pacman removes them