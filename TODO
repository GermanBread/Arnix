- Become distro-agnostic (need hooks for other package managers)
- git branch for stable and unstable instead of relying on releases (which are pretty much snapshots)
- updating from stable deletes arnix.conf